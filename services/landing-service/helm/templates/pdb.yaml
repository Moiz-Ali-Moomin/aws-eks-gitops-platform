{{- if .Values.podDisruptionBudget.enabled }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "landing-service.fullname" . }}
  labels:
    {{- include "landing-service.labels" . | nindent 4 }}
spec:
  minAvailable: 1
  selector:
    matchLabels:
      {{- include "landing-service.selectorLabels" . | nindent 6 }}
{{- end }}
