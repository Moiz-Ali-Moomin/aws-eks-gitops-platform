apiVersion: v1
kind: Service
metadata:
  name: {{ include "storefront-web.fullname" . }}
  labels:
    {{- include "storefront-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "storefront-web.selectorLabels" . | nindent 4 }}
