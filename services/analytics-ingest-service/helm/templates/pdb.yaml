{{- if .Values.podDisruptionBudget.enabled }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "analytics-ingest-service.fullname" . }}
  labels:
    {{- include "analytics-ingest-service.labels" . | nindent 4 }}
spec:
  minAvailable: 1
  selector:
    matchLabels:
      {{- include "analytics-ingest-service.selectorLabels" . | nindent 6 }}
{{- end }}
