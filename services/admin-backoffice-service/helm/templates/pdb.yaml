{{- if .Values.podDisruptionBudget.enabled }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "admin-backoffice-service.fullname" . }}
  labels:
    {{- include "admin-backoffice-service.labels" . | nindent 4 }}
spec:
  minAvailable: 1
  selector:
    matchLabels:
      {{- include "admin-backoffice-service.selectorLabels" . | nindent 6 }}
{{- end }}
